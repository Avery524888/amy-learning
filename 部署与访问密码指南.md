# 艾米的学习乐园 — 整站「访问密码」部署指南

## 一、为什么必须迁移托管

当前正式网址 `https://avery524888.github.io/amy-learning/` 托管在 **GitHub Pages**，它是**完全公开**的：

- 任何人只要知道链接就能直接打开；
- 页面的 HTML / JS / 全部学习数据（3000 词、3000 绘本、66 篇故事等）都能被下载；
- **GitHub Pages 没有「访问密码 / 权限」功能**，无法限制谁能用。

所以，想做到「限制别人使用这个网址」，必须在**托管层**加一道门禁。最省事、免费的方案是 **Netlify 的站点级密码（Password protection）**——不知道密码的人连页面都打不开，资源也不会公开。

> 之前讨论过的「短信登录」属于**应用层**遮挡：没登录看不到内容，但懂技术的人仍能量下载资源绕过。本次选的「整站密码」是**网络层**真限制，更彻底。两者可叠加，但通常整站密码已足够。

---

## 二、已为你准备好的产物

| 文件 | 说明 |
|------|------|
| `amy-learning-netlify.zip` | 已打包的完整静态站点（43 个文件，约 3.1MB），直接拖到 Netlify 即可 |
| `_deploy/` 目录 | 同上，未压缩版，也可直接拖文件夹 |
| `_deploy/netlify.toml` | Netlify 缓存配置（已随 zip 一并打包） |
| `deploy_netlify.py` | 可选：用 Netlify API 命令行一键部署（需你填 token） |

---

## 三、路径 A（推荐 · 零命令行 · 约 5 分钟）

1. **注册 Netlify 免费账号**：https://app.netlify.com/signup （可用 GitHub 一键登录，免费套餐即可开启密码保护）。
2. **拖拽部署**：打开 https://app.netlify.com/drop ，把 `amy-learning-netlify.zip`（或 `_deploy` 文件夹）直接拖进去。
3. **等待上传完成** → 会自动生成一个 `https://xxx.netlify.app` 的网址，点「Open site」确认内容正常。
4. **开启站点密码**：进入该站点后台 → `Site settings` → `Access control` → `Visitor access` → 找到 **Password protection** → 开启并设置密码（4–12 位，例如 `amy2026`）。
5. **分发**：把新网址 + 密码发给家人 / 自己使用；**旧 GitHub Pages 链接请停止分享**（它仍然公开，见下方说明）。

---

## 四、路径 B（可选 · 命令行一键部署）

如果你愿意用命令行：

- 安装 Node 后：`npm i -g netlify-cli` → `netlify login` → 在本项目目录运行 `netlify deploy --prod --dir _deploy`
- 或在 `deploy_netlify.py` 里填入 `NETLIFY_AUTH_TOKEN` 与 `NETLIFY_SITE_ID`，然后运行：
  ```
  python deploy_netlify.py
  ```
- 部署完成后，**仍需按路径 A 第 4 步在控制台开启密码**。

---

## 五、验证是否真的受限

- 用**无痕窗口**打开新网址 → 应弹出密码输入框；输错被拒，输对才进入。
- 确认旧 GitHub Pages 网址 `https://avery524888.github.io/amy-learning/` 仍可公开打开（它没被限制）——所以**只把新的 `netlify.app` 网址当正式入口**，旧链接不要再发给别人。
- 若想彻底关停旧链接：进入 GitHub 仓库 `Avery524888/amy-learning` → `Settings` → `Pages` → 把 Source 改成 `None`（或删除该仓库的 Pages 设置）。这样旧网址也会失效，进一步避免泄露。

---

## 六、备选：Cloudflare Access（邮箱一次性码门禁）

如果你已有**自定义域名**（如 `amyschool.xxx`）：

- 把站点挂到 Cloudflare Pages（连接 GitHub 仓库自动部署，或上传 `_deploy`）；
- 在 Cloudflare 后台对该域名开启 **Access**：访问时填邮箱 → 收一次性验证码 → 仅白名单邮箱可进。
- 比密码更严格（不怕密码外泄），但需要域名 + Cloudflare 账号，步骤略多。需要这一步的详细指引可随时让我补充。

---

## 七、小结

| 诉求 | 解决 |
|------|------|
| 限制别人打开网址 | Netlify 站点密码（网络层真限制）✅ |
| 不想注册新平台 | 也可用 Cloudflare Access（需域名）；GitHub Pages 本身做不到 |
| 旧链接还公开 | 停分享旧链接，或在 GitHub 关掉 Pages |
