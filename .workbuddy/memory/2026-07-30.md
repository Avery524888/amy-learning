# 2026-07-30 工作记录

## 任务：根据桌面「指令.docx」搭建儿童 AI 学习打卡平台
- 需求来源：用户桌面 `C:/Users/wangxinmiao/Desktop/指令.docx`（中文 docx，含表格）。
- 提取方法：docx 是 zip，用 python 解 `word/document.xml` 解析；中文文件名需在 git bash 下 `cp` 成 ASCII 名再处理，且控制台打印会乱码，须 `open(..., encoding='utf-8')` 写文件后用 Read 读取。

## 交付物（本工作区）
- `index.html` + `assets/style.css`（粉色可爱主题、左右框架、顶栏积分/打卡天数/每日鼓励语）
- `assets/data.js`（拼音/识字故事/计算/英语/逻辑/诗词300首样例/奖励规则/鼓励语）
- `assets/store.js`（localStorage 持久化：用户、积分、打卡、词库、每日上限计数）
- `assets/audio.js`（SpeechSynthesis 朗读 + webkitSpeechRecognition 评分，缺失时降级为「我读完了」手动确认）
- `assets/modules.js`（9 个模块：拼音/识字/计算/英语/逻辑/诗词/词库/奖励/日历打卡）
- `assets/app.js`（导航、顶栏、用户改名改头像弹窗、toast）

## 验证
- 5 个 JS 文件 `node --check` 通过。
- jsdom 运行时测试（node workspace 装了 jsdom）模拟加载+逐个渲染 9 模块+导航+改名+打卡，全部 PASS。
- 本地预览：`python -m http.server 8000`（127.0.0.1:8000）。

## 备注
- 英语短片为 CSS 动画占位（离线无视频资源）。
- 诗词库为样例集（跨诗人/类型可筛选），可按需扩充至 300 首。
- AI 对话为规则式英文鼓励回复（无真实 LLM 接入）。

## 公网部署
- 通过 CloudStudio 部署成功，公网分享链接：https://afbbf3394be240d087d0a15c58f31eeb.sh5.agentos-app.net（sandboxId: afbbf3394be240d087d0a15c58f31eeb）。

## 第二轮迭代（同日）
按用户要求完成三项增强：
1. **诗词库扩充到 320 首**：数据源 chinese-poetry 仓库 `全唐诗/poet.tang.N.json`（raw.githubusercontent，注意该仓库 JSON 实为 GBK 双重编码，需 `raw.decode('utf-8')` 即可得正确中文）。诗词数据生成脚本 /tmp/build_poems.py → 输出 assets/poems_data.js（window.__POEMS_RAW，含 type/diff 启发式分类）。
2. **拼音运行时生成**：引入 pinyin-pro（npm 装于 node workspace，拷贝 dist/index.mjs → assets/pinyin-pro.mjs，564KB）。index.html 用 `<script type=module>` 注入并设 window.pinyinPro；诗词模块用 `pinyin(line,{type:'array',nonZh:'removed'})` 按汉字逐字生成拼音。jsdom 测试无 ESM import，诗词降级渲染（无拼音）；真实浏览器正常。验证：node 直接 import 该 mjs 输出正确拼音数组。
3. **真实 AI 对话**：store 增加 ai 配置（baseURL/apiKey/model，localStorage）。顶栏新增 ⚙ 设置弹窗；英语对话模块 callAIDialogue() 调用 OpenAI 兼容 /chat/completions，未配置则降级内置回答。
4. **家长端学习报告**：新增「家长端」类目（👨‍👩‍👧），展示学习天数/连续打卡/累计积分/今日积分/词库数量/奖励收集/近7日积分柱状图。

验证：5 个 JS node --check 通过；jsdom 测试 10 模块全渲染 + AI 配置保存 + 家长端重渲染 PASS；pinyin-pro ESM 输出正确。
重新部署到同一 CloudStudio 沙箱（链接不变）。
