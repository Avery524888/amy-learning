# 2026-08-13 工作日志

## 线上地址再次不可达（紧急）
- 用户反馈公网地址 https://cdc4f727d14f4471b6af27b5db85b20e.bj3.agentos-app.net/standalone.html 又打不开。
- 诊断：curl 超时 `http_code=000`（沙箱再次掉线）；`workbuddy_cloudstudio_deploy` 连续 4 次返回平台侧 **504 upload failed**（上传服务故障，非代码）。`_deploy` 目录干净（仅 standalone.html 996K），排除本地原因。
- 本地 `standalone.html` 完好（1019157 字节，全部功能标记齐全）。

## 处理
- 新建自动化 `automation-1786611191381`「艾米学习乐园-定时恢复线上地址」：每 2 小时自动重推 `_deploy`，平台恢复即自动救活线上地址（仅成功时简要提示）。
- 向用户交付本地 `standalone.html`（完全自包含单文件）作为立即可用的兜底：直接双击用浏览器打开即可，无需服务器。
- 更新 MEMORY.md 部署坑：补充「沙箱周期性掉线 + 504 上传故障 + 自动重部署 + 本地文件兜底」四条。

## 待平台恢复后
- 自动化会自动重推；届时线上地址恢复，用户硬刷新即可见最新（含每日场景参考 / 跟读停止修复 / 移动端地图放大 等 08-12 的功能）。
