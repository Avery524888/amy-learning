# 2026-08-18 工作日志

## 连接 edgeone-pages 连接器（稳定公网托管）
- 用户要求连接 `edgeone-pages`（EdgeOne Makers）连接器，以获取永久固定、国内 CDN 加速的公网地址，替代反复掉线的 CloudStudio 沙箱。
- 现状：连接器状态为 `disconnected`；本地代理 `connector-proxy`(http://127.0.0.1:51488/mcp) 聚合 MCP 服务器为 none。OAuth 授权必须由用户在 WorkBuddy 客户端完成，AI 无法代点。
- 已向用户给出 UI 连接步骤（连接器面板 → EdgeOne Pages → 连接 → 腾讯云授权）。
- 部署包 `_deploy/index.html` 已确认是最新 standalone 构建（含 invalidateStaleModules / china-macau-hit / book-new-badge / getDailyBooks / 我的词库 等标记），连接后直接部署即可。
- 待办：用户连上后，调用 edgeone-pages 部署工具把 `_deploy` 推上去，拿到固定网址。
