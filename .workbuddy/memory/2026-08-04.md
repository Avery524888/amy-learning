# 2026-08-04 工作记录

## 任务：按新指令文档重建「艾米的学习乐园」学习平台
用户给了新文档 `学习平台.docx`（桌面），要求：命名「艾米的学习乐园」、粉色可爱风、左右框架、响应式、清晰架构/合理数据结构/扩展性/安全性。

### 关键改动（相对 7/30 初版）
- 新增两大模块：**绘本**（4 本翻页绘本）、**游戏**（记忆翻牌/颜色找一找/数数闯关）。
- 识字：长按生字 → 弹窗「加入词库」；文字连线小游戏（字↔拼音）。
- 英语：长按单词卡 → 加入英语词库；保留 AI 对话（需 ⚙️ 设置接口）。
- 诗词：补全到 **395 首**，分两层数据源（见下）；每首含 ≥100 字儿童向释义、已学/未学标记、「我会背诵啦~」+5 花。
- 奖励：改为**两层兑换**（红花→实物；树苗/珍珠/猫→体验奖励），含糖果/电视/2元/游乐场。
- 词库：汉字 / 英语 / 诗词（已学·会背诵）三块 + 自动字数。
- 家长端：连续打卡、累计、今日、已学汉字/单词/诗词、近7日柱状图、奖励收集。
- 顶部栏：总积分/今日/打卡天数/每日鼓励语。

### 诗词数据管线（重要经验）
- 开源 `chinese-poetry` 的《全唐诗》仅 `poet.tang.0.json`（1000 首，从太宗皇帝开篇），**不含**静夜思/咏鹅等儿童名篇（那些出自《唐诗三百首》）。
- 数据集为**繁体**，用 `opencc-python-reimplemented`(t2s) + 手动异体字补丁转简体。venv 路径：`C:/Users/wangxinmiao/.workbuddy/binaries/python/envs/default`。
- 手工补录 **75 首儿童必读名篇**（李白/杜甫/白居易/杜牧/王维等），合计 395 首。
- 拼音运行时用 `pinyin-pro`（564KB ESM，assets/pinyin-pro.mjs），由 `pinyin_init.js`（独立 module，避免 CSP 拦截内联脚本）挂载到 window.pinyinPro。
- 释义：`build_notes.py` 为 75 首名篇写详细解读；其余用真实诗人生卒年生成导读回退（`getPoemNote`）。

### 安全/架构
- index.html 加 CSP meta（script-src 'self'，无 unsafe-inline；connect-src 'self' https: 供 AI）。
- store.js：localStorage 持久化 + 版本迁移 + `esc()` 防 XSS + 用户名白名单校验 + 每日积分清零/历史。
- 响应式：桌面左右框架；≤860px 侧边栏变抽屉（汉堡+遮罩）。

### 验证
- 6 个 JS `node --check` 通过；jsdom 测试 12 模块全 PASS、交互（背诵/打卡/兑换/加词库）无报错；本地 HTTP 200。

### 文件
- 重写：index.html, assets/{style.css,data.js,store.js,audio.js,modules.js,app.js}
- 数据：assets/poems_data.js(395), assets/poem_notes.js(75详解), assets/pinyin-pro.mjs, assets/pinyin_init.js
- 数据生成脚本：build_curated.py, build_notes.py（保留作管线文档）
