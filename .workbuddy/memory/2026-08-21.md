## 2026-08-21

- 22:20 修复绘本馆「今日新绘本」推荐卡片布局异常：原 featured 为撑满宽度的 `.card` 横幅，内容挤在左侧、右侧大量空白。改为 `.book-featured` 固定宽度居中卡片（左侧封面 + 右侧标题/说明），新标签和已读对钩绝对定位。
- 修改 `assets/style.css` 新增 `.book-featured`、`.book-featured .book-cover`、`.book-featured-info`、`.book-featured-title`、`.book-featured-desc` 等样式。
- 修改 `assets/modules.js` book() 模块：初始 `#featured` 改为 `book-featured` 类；`renderList()` 中使用新的 featured 卡片结构。
- 重新构建 `standalone.html` 并通过 `test_smoke.js`（SMOKE_PASS）。
- 部署到 GitHub Pages：`https://avery524888.github.io/amy-learning/`（HTTP 200，Content-Length 1066755）。
- 22:27 升级绘画模块「今日场景参考」为手绘风格 SVG：参考用户提供的城堡/西瓜冰淇淋/小桥房子/兔子胡萝卜/蘑菇屋示例，新增 8 幅粗黑边线 + 鲜艳色块 + 卡通主体居中 + 丰富背景的场景（彩虹城堡、西瓜冰淇淋、小桥房子、兔子菜园、蘑菇小屋、海底乐园、星空露营、花园蝴蝶），保留并复用原有的「换一换」按钮逻辑（点击随机切换且不重复当前场景）。通过 `test_smoke.js`（SMOKE_PASS）并重新部署 GitHub Pages（HTTP 200，Content-Length 1067505）。
- 22:38 用户反馈 SVG 仍太生硬、色彩单一，要求完全像示例的手绘位图效果。改为使用 ImageGen 生成 8 张儿童手绘风格位图（1024×768），经 sharp 压缩为 800×600 JPEG（单张 82–134KB），以内联 base64 `data:image/jpeg;base64,...` 形式替换 `DRAW_SCENES`。修改 `assets/modules.js` 的 `renderScene()` 用 `<img>` 展示场景，更新 `test_smoke.js` 检查整幅图为 `<img>`。`standalone.html` 增至约 1.98MB，仍自包含。重新构建、测试（SMOKE_PASS）、部署 GitHub Pages（HTTP 200，Content-Length 1070967）。
