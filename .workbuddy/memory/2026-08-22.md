# 2026-08-22 工作记录

## 范画池大扩充（绘画模块）
- 用户反馈：范画只有 14 张太少，要求不限制张数、每天推荐不同内容。
- 改动 `assets/data.js` 的 `DRAW_PROMPTS`：从 14 → **117** 个主题，按 8 大类目（动物/食物/自然/交通/物品/人物/节日/海洋天空）组织；每主题新增 `cat`（类目）与 `step`（儿童友好绘画小提示）。原 14 个名称保留以匹配 `DRAW_REFS` 的 SVG 线稿；新主题无 SVG 时回退为大 emoji 目标图 + 小提示。
- 改动 `assets/modules.js` 的 `draw()`：每日范画选取由 `S.dailyPickN`（连续索引，相邻天 4/5 重叠）改为基于 `S.todaySeed()` 的 **seededShuffle**，每天取前 6 个，保证每天分散且真正不同。原 14 个 SVG 仍显示线稿；新增类目徽标 `.draw-ref-cat` 与选中提示 `#activeRefTip`（显示 `step`）。每日可画上限 5 → 6。
- 样式：`style.css` 加 `.draw-ref-cat`、`.draw-ref-tip-big`。
- 已 `build_standalone.py` + `deploy_github_pages.py` 部署，curl 校验线上 `data.js` 范画池 = 117，已含新主题（长颈鹿/机器人/鲸鱼等）。地址：https://avery524888.github.io/amy-learning/
- 后续若要继续扩充范画：直接往 `DRAW_PROMPTS` 追加 `{name,emoji,cat,step}` 即可，逻辑无需改；想要某主题也有手绘线稿就在 `DRAW_REFS` 加同名 SVG。

## 十万个为什么：加「换一换」+ 题库大扩充
- 用户反馈：十万个为什么模块也要「换一换」，每次更新内容不重复。
- 改动 `assets/data.js` 的 `WHYS`：原 43 条保留，追加约 78 条全新儿童科普问答（动物/自然/人体/食物植物/科技太空/生活），总量 **121** 条，均与原文不重复。
- 改动 `assets/modules.js` 的 `whys()`：① 每日选取由 `S.dailyPickN(D.WHYS,5)`（连续索引会跨天重叠）改为基于 `S.todaySeed()` 的 **seededShuffle**，每天取前 **6** 条，保证每天不同；② 新增 `shownQ` 记录 + `#whyShuffle` 按钮触发 `shuffleWhys()`，从全池排除已展示项取 6 条、池耗尽再重置，做到「每次换一换都不重复」；③ 卡片保留 🔊 读给我听。
- 样式：`style.css` 加 `.why-head`（标题行）、`.why-count`、`.why-shuffle`（紫色换一换按钮，复用 .btn）。
- 已构建部署，curl 校验线上 `WHYS = 121`、换一换逻辑上线（7 处命中）。地址：https://avery524888.github.io/amy-learning/
- 复用了绘画模块 `draw()` 的同款 seededShuffle 模式；两个模块的「每日不同 + 换一换不重复」逻辑现已一致。
