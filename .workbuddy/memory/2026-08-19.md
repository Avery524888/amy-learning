# 2026-08-19 工作日志

## EdgeOne Pages 连接器授权超时（持续排查）
- 用户反馈：连接 `edgeone-pages` 一直「授权超时，请重试」，**重启电脑也没用** → 排除客户端进程残留，判定为网络环境挡 OAuth 回调 或 平台侧连接器故障。
- 诊断结论：本地代理 127.0.0.1:51488 此前已确认健康；CAM 无 WorkBuddy/EdgeOne 角色属正常（token 存 WorkBuddy 侧，不落地 CAM）。
- 给用户新建议：① 换手机热点联网再连（定位是否网络挡回调）；② 盯授权弹窗地址栏卡住的回跳网址发我定位；③ 关 VPN/代理/拦截插件；④ 连别的连接器（GitHub 等）隔离是否 EdgeOne 专属。
- 现状公网地址：CloudStudio `https://55e52ddd65d340dea60788f3931c122f.app.workbuddy.link` 经 curl 确认仍存活（1,055,728 字节=最新构建），每 2 小时自动重推自动化兜底。
- 待办：用户若提供授权弹窗卡住的回跳网址则深入定位；若热点可连则 EdgeOne 即成；否则维持 CloudStudio 临时地址 + 本地 standalone.html。

## EdgeOne 授权超时终极结论 → 放弃，改用 GitHub Pages 成功
- WebSearch 查证：**EdgeOne Pages MCP 连接器授权超时是平台侧普遍问题**（腾讯云开发者社区多人反馈 deploy_folder/deploy 超时 60s，MCP tool 和 CLI 均失败），非用户账号/网络/客户端问题。
- 用户换手机热点连接仍超时 → 坐实非本地网络问题。
- 决策：放弃 EdgeOne，改走 **GitHub Pages**。
- 用户在 WorkBuddy 连 `github` 连接器成功，但该连接器仅暴露 Ardot 资源、不提供 MCP 工具 → 改用 **GitHub API + Personal Access Token** 方案。
- 用户名 `Avery524888`，Token 已拿到（`ghp_ytbT…`，存于 `deploy_github_pages.py`）。

## GitHub Pages 部署成功（永久固定地址）
- 创建仓库 `Avery524888/amy-learning`（public）。
- 通过 GitHub Git Database API：base tree + new tree(index.html=standalone.html 1055728B) + commit + 创建 `gh-pages` 分支。
- 启用 GitHub Pages，source=gh-pages 分支根目录。
- 构建状态：5 秒内 `built`。
- 内容验证：HTTPS 拉取 1,055,728 字节，含全部功能标记（艾米的学习乐园/invalidateStaleModules/china-macau-hit/毛泽东/我的词库）。
- **永久地址：https://avery524888.github.io/amy-learning/**
- 创建 `deploy_github_pages.py` 一键部署脚本：以后每次功能更新只需 `python deploy_github_pages.py` 即可重推，无需 CloudStudio、不会掉线、地址永久不变。
- 已更新 MEMORY.md：主地址改为 GitHub Pages，CloudStudio 降级为备份。
