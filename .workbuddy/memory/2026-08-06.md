# 2026-08-06 学习平台第二轮 bug 修复

修复 8 项问题并重新部署：
1. 拼音字母改读中文例字（b→波…），避免 TTS 把拉丁字母读成英语。
2. audio.js 语音打分：优先在线自然女声/儿童音；中文 Xiaoxiao/Yaoyao，英文 Ana/Aria/Jenny/Sonia。
3. 词库点击 ✕ 后立即重新渲染，词条即时消失。
4. 识字故事增大字间距（letter-spacing:0.18em）与热区；重写 attachLongPress，加入 8px 移动容差，减少误触。
5. 诗词拼音启用 pinyin-pro segment:true，多音字按上下文自动选音。
6. 计算十格法：空格子点击添加小动物，再次点击可取消。
7. 逻辑题输入框旁加 🎤 语音输入按钮，识别后自动填入并查看答案。
8. 诗词全文朗读改用 speakPoem，语速 0.56、音调自然，方便孩子听清。

验证：node --check 通过；jsdom 渲染 12/12 模块无错；standalone.html 单文件 640KB，已部署同一 CloudStudio 链接。

# 2026-08-06 学习平台第三轮优化

完成 6 大项并重新部署到同一 CloudStudio 链接：
1. 每日表现模块：8 项行为（好/坏各带红花分值），按天存 perfByDate，setPerf 算净额调 adjustFlowers（可扣减，下限0），家长端新增统计卡。
2. 每日单词：enCursor + dailyEN 缓存，每天 3 新 + 2 复习（跨天重置），英语卡片分 newKeys/reviewKeys 两网格。
3. 内容每日轮换：todaySeed()/seededRand()/dailyPick()/dailyPickN() 基于 2026-01-01 起天数做确定性种子，同天稳定、跨天变化；拼音组合、绘本推荐、诗词随机均改走每日种子。
4. 游戏替换：原"找颜色"改为"两图找不同"（DIFF_SCENES 3 场景，4×4 双板，点右图不同处 +2 花）。
5. 绘画模块：Canvas 2D（pointer 画笔描边 + 洪水填充 floodFill 4 连通扫描线涂色板），历史倒序封存（toDataURL 最多30），每部作品给积极评语+完善建议，支持撤销/清空。
6. 诗词配图：POEM_IMG_KEYWORDS 约120 关键词→emoji，genPoemImages 每句生成插画卡（poem-img-card）结合句意显示。

验证：node --check 全通过；jsdom 渲染 14/14 模块零错误（表现 good→perfTotal=2、dailyEN 3+2、诗词配图卡3张、找不同右板16格、绘画8色板）；curl 线上 HTTP 200、870005 字节、6 项标记齐全。
注意：绘画/录音依赖真机浏览器能力（Canvas/getContext、Web Speech），jsdom 无法覆盖，需真机验证。

# 2026-08-06 学习平台第四轮优化（3 项）

1. 诗词释义一键朗读：释义 div 加 #pNoteRead 按钮，调用 A.speak(note,"zh-CN",{rate:0.8}) 朗读整段释义（modules.js 诗词 render 处）。
2. 每日英语扩到 5 新 + 2 复习：store.js getDailyEN 新词循环 3→5、enCursor 步进 3→5、复习偏移 -3→-5；english() 文案同步改「5 个新单词」。
3. 绘画布局重构 + 简笔画范本：data.js 新增 DRAW_REFS（14 主题线条稿 SVG，viewBox 0 0 200 200、stroke 描边 + 浅填充）；draw() 改为「上方画笔工具区(.draw-top) / 左侧范画(.draw-ref 显示对应 SVG) / 右侧画板(.draw-canvas-wrap) / 底部历史」；style.css 同步新增 .draw-top/.draw-main/.draw-ref* 等，移除 .draw-left/.draw-right。

验证：node --check 三文件通过；jsdom 渲染 13 项断言全 PASS（dailyEN 5+2、DRAW_REFS=14 且每个 prompt 都有 SVG、english 新卡5/复习2、poem 含 #pNoteRead 与配图卡、draw 含 .draw-top/.draw-main/范本SVG/#drawCanvas/#drawHist）；curl 线上 HTTP 200、878645 字节、6 项标记齐全。重用同一 CloudStudio 沙箱 ff27e10f。
