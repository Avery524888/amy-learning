# 2026-08-10 工作日志

## 完成：艾米学习乐园 7 项功能更新（用户 2026-08-17 提的清单，本次集中实现）

1. **英语每日5新词持久化**（请求①）：`english()` 改为使用 `S.getDailyEN()` 固定当天 5 个新词；加入词库后仍留在「今日新学」并打「已学会✓」，刷新后保持新增状态。复习记录=已学会且不在今日新学里的词。
2. **绘本馆每日5新+已读倒序**（请求②）：`store.getDailyBooks()` 新增（5本/天，与近7天不重复）；绘本网格前4本带「新」标、featured 卡片再带1个，共5本新在前面；已读绘本按 `markBookRead` 记录的 `ts` 倒序排在前面（store.readBooks 改为存 {title,ts}，兼容旧字符串）。
3. **地图澳门放大+珠三角容错区**（请求③）：`chinaMapGame()` 在澳门(591,471)叠加透明扩大圈(r=16)+珠江三角洲容错圈(r=42)+脉冲提示环；仅当挑战目标为「澳门」时激活 pointer-events，点击即判正确；非澳门时圈透明穿透，不影响广东/香港等正常点击。
4. **十万个为什么每日5条**（请求④）：原本已 `dailyPickN(D.WHYS,5)` 按天轮换；本次将 WHYS 池从 25 扩到 45 条，更新更丰富。
5. **词库实时同步**（请求⑤）：`notifyBankChange()` 现把 `wordbank` 也纳入依赖列表，并统一「清除相关模块缓存」（不再强制整模块重绘，避免打断绘本阅读器）；`wordPopup` 加入成功后、`poetry` 背诵成功后均调用之，使词库/识字/英语/诗词下次访问即刷新。
6. **诗词量补齐**（请求⑥）：新建 `assets/poems_extra.js`（义务教育必背+李白/杜甫/白居易/杜牧/王维/苏轼+毛泽东12首等约110首），运行时去重追加；总量 395→471，0 重复。已加入 `build_standalone.py` 内联列表。
7. **去除长按选区干扰**（请求⑦）：`style.css` 对 `.chr/.poem-char/.word-card/.book-cover/.book-name` 加 `user-select:none; -webkit-touch-callout:none`，长按只弹「加入词库」窗，不再出现截取/复制菜单。

## 验证
- `node --check` 四个 JS 文件全过。
- 重建 `standalone.html`（819KB→1.05MB，无外部资源引用）。
- `test_smoke.js`：SMOKE_PASS（18 项全绿）。
- 新增 jsdom 专项验证：Macau 三圈存在、每日新绘本=5、已读倒序、英语加入后仍在新学且打勾、wordbank 缓存清除 → VERIFY_PASS。

## 部署
- 旧沙箱 cdc4f… 已过期；本次重推 `_deploy` 生成**新沙箱**：https://55e52ddd65d340dea60788f3931c122f.app.workbuddy.link （curl 已确认服务新构建，含 毛泽东/china-macau-hit/book-new-badge 等标记）。
- 已同步更新 MEMORY.md 部署段与诗词总量。
- 仍建议：线上挂掉时用户可本地双击 `standalone.html`（0 外部请求，完全自包含）。EdgeOne 稳定托管仍因连接器未连接而搁置。
